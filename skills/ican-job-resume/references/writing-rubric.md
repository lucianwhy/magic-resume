# 简历改写规则

- 采用“场景 + 行动 + 技术/方法 + 已证实结果”表达；没有结果证据时，在行动处收束。
- 用具体动词：设计、实现、搭建、协同、验证、部署、维护；避免“精通、顶尖、行业领先、显著提升”等无证据词。
- 先写最匹配 JD 的项目、职责和技术；删减重复或不相关内容，不删除用户要求保留的事实。
- 数字、百分比、用户量、并发量、成本和排名必须有独立 `fact_id`。
- 同一事实不要在技能、项目、经历中重复三次；技能写覆盖面，项目写实际应用。
- 每个候选改写附 `fact_ids`；无 `fact_ids` 的内容默认不可写入。
